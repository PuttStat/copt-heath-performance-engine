import type { ReactNode } from "react";
import Link from "next/link";
import { AuthGate } from "./auth-gate";
const nav = [["today", "Today", "/"], ["rounds", "Rounds", "/rounds/new"], ["performance", "Performance", "/performance"], ["practice", "Practice", "/practice"]] as const;
export function AppShell({ active, children }: { active: string; children: ReactNode }) { return <AuthGate><div className="app-frame"><header className="topbar"><Link className="brand" href="/" aria-label="Vector Golf Performance home"><span className="brand-mark">V</span><span>VECTOR <small>GOLF PERFORMANCE</small></span></Link><nav aria-label="Primary navigation">{nav.map(([id, label, href]) => <Link className={active === id ? "active" : ""} href={href} key={id}>{label}</Link>)}</nav><div className="profile"><span><strong>Selected player</strong><small>Beta access</small></span><b>VG</b></div></header><main className="main-content">{children}</main><nav className="mobile-nav" aria-label="Mobile navigation">{nav.map(([id, label, href]) => <Link className={active === id ? "active" : ""} href={href} key={id}><span>{id === "today" ? "⌂" : id === "rounds" ? "+" : id === "performance" ? "↗" : "◇"}</span>{label}</Link>)}</nav></div></AuthGate>; }
