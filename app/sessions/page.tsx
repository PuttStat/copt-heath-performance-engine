"use client";
import{useCallback,useEffect,useMemo,useState}from"react";import{AppShell}from"../ui/app-shell";import{getSupabaseBrowserClient}from"../../lib/supabase/client";import{usePlayerData}from"../../lib/use-player-data";
type Player={id:string;display_name:string|null;email:string};type Programme={id:string;player_id:string;status:string};type Week={id:string;week_number:number;phase:string;focus:string;golf_minutes:number;vector_minutes:number;status:string};type Item={id:string;code:string;title:string;item_type:"golf_drill"|"vector_exercise";purpose:string;dosage:string|null};type Block={id:string;sequence:number;domain:"golf"|"vector";stage:string;minutes:number;instructions:string|null;success_criterion:string|null;library_item_id:string|null;source_case_id:string|null;library_items:Item|null};type Session={id:string;session_number:number;title:string;scheduled_day:string|null;objective:string|null;status:string;session_blocks:Block[]};
const stageByPhase:Record<string,string[]>={Measure:["baseline","technique","skill"],Build:["technique","skill","random"],Stabilise:["skill","random","pressure"],Transfer:["random","pressure","transfer"],Perform:["pressure","transfer","baseline"]};
const split=(total:number,count:number)=>Array.from({length:count},(_,i)=>Math.floor(total/count)+(i<total%count?1:0));
export default function SessionsPage(){const{profile}=usePlayerData(),[players,setPlayers]=useState<Player[]>([]),[selectedId,setSelectedId]=useState(""),[programme,setProgramme]=useState<Programme|null>(null),[weeks,setWeeks]=useState<Week[]>([]),[weekId,setWeekId]=useState(""),[sessions,setSessions]=useState<Session[]>([]),[library,setLibrary]=useState<Item[]>([]),[approvedItems,setApprovedItems]=useState<Array<{item:Item;caseId:string}>>([]),[message,setMessage]=useState("");const canCoach=profile?.role==="coach"||profile?.role==="admin";const targetId=canCoach?selectedId:profile?.id||"";const week=weeks.find(item=>item.id===weekId);
const loadPlayers=useCallback(async()=>{const sb=getSupabaseBrowserClient();if(!sb||!profile||!canCoach)return;const{data:links}=await sb.from("coach_player_links").select("player_id").eq("coach_id",profile.id);const ids=(links||[]).map(x=>x.player_id);if(!ids.length)return;const{data}=await sb.from("profiles").select("id,display_name,email").in("id",ids);const rows=(data||[])as Player[];setPlayers(rows);setSelectedId(current=>current||rows[0]?.id||"")},[profile,canCoach]);
const loadBase=useCallback(async()=>{const sb=getSupabaseBrowserClient();if(!sb||!targetId)return;const{data:p}=await sb.from("programmes").select("id,player_id,status").eq("player_id",targetId).in("status",canCoach?["draft","published","completed"]:["published","completed"]).order("created_at",{ascending:false}).limit(1).maybeSingle();setProgramme(p as Programme|null);if(!p){setWeeks([]);return}const[{data:w},{data:l},{data:c}]=await Promise.all([sb.from("programme_weeks").select("id,week_number,phase,focus,golf_minutes,vector_minutes,status").eq("programme_id",p.id).order("week_number"),sb.from("library_items").select("id,code,title,item_type,purpose,dosage").eq("status","approved").order("code"),sb.from("diagnostic_cases").select("id,case_recommendations(library_items(id,code,title,item_type,purpose,dosage))").eq("player_id",targetId).eq("status","approved")]);const weekRows=(w||[])as Week[];setWeeks(weekRows);setWeekId(current=>current&&weekRows.some(x=>x.id===current)?current:weekRows[0]?.id||"");setLibrary((l||[])as Item[]);const approved:Array<{item:Item;caseId:string}>=[];for(const diagnostic of c||[])for(const rec of diagnostic.case_recommendations||[]){const raw=rec.library_items as unknown;const item=(Array.isArray(raw)?raw[0]:raw)as Item|null;if(item&&!approved.some(entry=>entry.item.id===item.id))approved.push({item,caseId:diagnostic.id})}setApprovedItems(approved)},[targetId,canCoach]);
const loadSessions=useCallback(async()=>{const sb=getSupabaseBrowserClient();if(!sb||!weekId)return;const{data}=await sb.from("programme_sessions").select("id,session_number,title,scheduled_day,objective,status,session_blocks(id,sequence,domain,stage,minutes,instructions,success_criterion,library_item_id,source_case_id,library_items(id,code,title,item_type,purpose,dosage))").eq("programme_week_id",weekId).order("session_number");setSessions((data||[])as unknown as Session[])},[weekId]);useEffect(()=>{const t=setTimeout(()=>void loadPlayers(),0);return()=>clearTimeout(t)},[loadPlayers]);useEffect(()=>{const t=setTimeout(()=>void loadBase(),0);return()=>clearTimeout(t)},[loadBase]);useEffect(()=>{const t=setTimeout(()=>void loadSessions(),0);return()=>clearTimeout(t)},[loadSessions]);
const totals=useMemo(()=>sessions.flatMap(s=>s.session_blocks||[]).reduce((sum,b)=>{sum[b.domain]+=b.minutes;if(!b.library_item_id)sum.missing++;return sum},{golf:0,vector:0,missing:0}),[sessions]);const balanced=!!week&&totals.golf===week.golf_minutes&&totals.vector===week.vector_minutes&&totals.missing===0;
const buildWeek=async()=>{const sb=getSupabaseBrowserClient();if(!sb||!week||!canCoach)return;if(sessions.length){setMessage("This week already has sessions. Remove or edit them instead of rebuilding.");return}const golfItems=approvedItems.filter(x=>x.item.item_type==="golf_drill"),vectorItems=approvedItems.filter(x=>x.item.item_type==="vector_exercise");const sessionCount=3,golfSplit=split(week.golf_minutes,sessionCount),vectorSplit=split(week.vector_minutes,Math.min(2,sessionCount));const{data:created,error}=await sb.from("programme_sessions").insert(Array.from({length:sessionCount},(_,i)=>({programme_week_id:week.id,session_number:i+1,title:`${week.phase} session ${i+1}`,objective:week.focus,status:"draft"}))).select("id,session_number");if(error||!created){setMessage(error?.message||"Could not create sessions.");return}const stages=stageByPhase[week.phase]||["skill","random","transfer"];const blocks:Array<Record<string,unknown>>=[];created.forEach((session,i)=>{const selected=golfItems[i%Math.max(golfItems.length,1)];if(golfSplit[i]>0)blocks.push({session_id:session.id,sequence:1,domain:"golf",stage:stages[i%stages.length],minutes:golfSplit[i],library_item_id:selected?.item.id||null,source_case_id:selected?.caseId||null,instructions:selected?.item.purpose||"Coach to assign an approved drill.",success_criterion:"Record the agreed success measure before progressing."});if(i<vectorSplit.length&&vectorSplit[i]>0){const v=vectorItems[i%Math.max(vectorItems.length,1)];blocks.push({session_id:session.id,sequence:2,domain:"vector",stage:"vector",minutes:vectorSplit[i],library_item_id:v?.item.id||null,source_case_id:v?.caseId||null,instructions:v?.item.purpose||"Assign Vector support only after a demonstrated movement requirement.",success_criterion:"Movement quality retained without unnecessary fatigue."})}});const{error:blockError}=await sb.from("session_blocks").insert(blocks);setMessage(blockError?blockError.message:golfItems.length?"Week built from the coach-approved prescription.":"Week built with unassigned blocks. Select approved library items before release.");await loadSessions()};
const updateBlock=async(id:string,changes:Partial<Block>)=>{const sb=getSupabaseBrowserClient();if(!sb)return;const{error}=await sb.from("session_blocks").update(changes).eq("id",id);setMessage(error?error.message:"Session block updated.");if(!error)await loadSessions()};const updateSession=async(id:string,changes:Partial<Session>)=>{const sb=getSupabaseBrowserClient();if(!sb)return;const{error}=await sb.from("programme_sessions").update({...changes,updated_at:new Date().toISOString()}).eq("id",id);setMessage(error?error.message:"Session updated.");if(!error)await loadSessions()};const release=async()=>{const sb=getSupabaseBrowserClient();if(!sb||!week||!programme)return;const{error}=await sb.from("programme_weeks").update({status:"published",updated_at:new Date().toISOString()}).eq("id",week.id);if(!error&&programme.status==="draft")await sb.from("programmes").update({status:"published",published_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("id",programme.id);setMessage(error?error.message:"Week released to the player.");if(!error)await loadBase()};
const selectable=(domain:"golf"|"vector")=>library.filter(item=>item.item_type===(domain==="golf"?"golf_drill":"vector_exercise"));return <AppShell active="sessions"><header className="page-heading"><div><p className="eyebrow">Package 7F.2 · Session builder</p><h1>Every minute has a purpose.</h1><p>Build from coach-approved evidence, progress through staged practice and release only when golf and Vector minutes reconcile exactly.</p></div></header>{message&&<div className="success-banner" role="status">{message}</div>}{canCoach&&<section className="session-player"><label>Player<select value={selectedId} onChange={e=>setSelectedId(e.target.value)}>{players.map(p=><option value={p.id} key={p.id}>{p.display_name||p.email}</option>)}</select></label></section>}{programme&&<section className="week-tabs">{weeks.map(w=><button className={w.id===weekId?"active":""} onClick={()=>setWeekId(w.id)} key={w.id}><b>{w.week_number}</b><span>{w.phase}</span></button>)}</section>}{week&&<section className="reconcile-bar"><div><span>Golf</span><strong>{totals.golf}/{week.golf_minutes}</strong></div><div><span>Vector</span><strong>{totals.vector}/{week.vector_minutes}</strong></div><div><span>Assignments</span><strong>{totals.missing?`${totals.missing} missing`:"Complete"}</strong></div><b className={balanced?"balanced":"unbalanced"}>{balanced?"Ready to release":"Reconciliation required"}</b>{canCoach&&!sessions.length&&<button onClick={buildWeek}>Build suggested week</button>}{canCoach&&sessions.length>0&&<button disabled={!balanced} onClick={release}>Release week</button>}</section>}<section className="session-builder">{sessions.map(session=><article className="planned-session" key={session.id}><header><span>Session {session.session_number}</span>{canCoach?<><input value={session.title} onChange={e=>setSessions(all=>all.map(s=>s.id===session.id?{...s,title:e.target.value}:s))} onBlur={e=>updateSession(session.id,{title:e.target.value})}/><input placeholder="Scheduled day" value={session.scheduled_day||""} onChange={e=>setSessions(all=>all.map(s=>s.id===session.id?{...s,scheduled_day:e.target.value}:s))} onBlur={e=>updateSession(session.id,{scheduled_day:e.target.value})}/></>:<><h2>{session.title}</h2><small>{session.scheduled_day}</small></>}</header><div>{(session.session_blocks||[]).sort((a,b)=>a.sequence-b.sequence).map(block=><div className={`planned-block ${block.domain}`} key={block.id}><span>{block.stage}</span><div>{canCoach?<select value={block.library_item_id||""} onChange={e=>{const item=library.find(x=>x.id===e.target.value);void updateBlock(block.id,{library_item_id:e.target.value||null,instructions:item?.purpose||block.instructions})}}><option value="">Assign approved library item</option>{selectable(block.domain).map(item=><option value={item.id} key={item.id}>{item.code} · {item.title}</option>)}</select>:<h3>{block.library_items?.title}</h3>}<p>{block.instructions}</p>{block.library_items?.dosage&&<small>{block.library_items.dosage}</small>}</div>{canCoach?<label>Minutes<input type="number" min="1" max="300" value={block.minutes} onChange={e=>setSessions(all=>all.map(s=>({...s,session_blocks:s.session_blocks.map(b=>b.id===block.id?{...b,minutes:Number(e.target.value)}:b)})))} onBlur={e=>updateBlock(block.id,{minutes:Number(e.target.value)})}/></label>:<strong>{block.minutes} min</strong>}</div>)}</div></article>)}{programme&&!sessions.length&&<div className="empty-state compact"><h2>No sessions built for this week</h2><p>{canCoach?"Build the suggested week, then verify every library assignment and minute before release.":"Your coach has not released this week yet."}</p></div>}{!programme&&<div className="empty-state compact"><h2>No programme available</h2><p>Create and publish the 12-week programme foundation before building sessions.</p></div>}</section></AppShell>}
