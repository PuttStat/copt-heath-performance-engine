"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { AppShell } from "../ui/app-shell";
import { getSupabaseBrowserClient } from "../../lib/supabase/client";
import { usePlayerData } from "../../lib/use-player-data";
import { buildDiagnosticObservations, type MissAggregate } from "../../lib/diagnostic-engine";
import {
  recommendPlanItems,
  recommendationForSession,
  splitMinutes,
  type ApprovedPrescription,
  type RecommendationLibraryItem,
} from "../../lib/programme-recommendation";

type Player={id:string;display_name:string|null;email:string};
type Intake={player_id:string;primary_goal:string;outcome_target:string;weekly_golf_minutes:number;weekly_vector_minutes:number;sessions_per_week:number;available_days:string[];facilities:string[];competition_dates:string;recovery_constraints:string;consent_confirmed:boolean};
type Programme={id:string;player_id:string;coach_id:string;title:string;primary_goal:string;start_date:string;status:"draft"|"published"|"completed"|"archived";planning_mode:"coach_led"|"self_directed";golf_minutes_per_week:number;vector_minutes_per_week:number;current_week:number};
type Week={id:string;week_number:number;phase:"Measure"|"Build"|"Stabilise"|"Transfer"|"Perform";focus:string;golf_minutes:number;vector_minutes:number;review_type:string|null;coach_notes:string|null;status:string};
const days=["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"];
const facilities=["Driving range","Short-game area","Putting green","Golf course","Gym","Home equipment","Launch monitor"];
const stageByPhase:Record<string,string[]>={Measure:["baseline","technique","skill"],Build:["technique","skill","random"],Stabilise:["skill","random","pressure"],Transfer:["random","pressure","transfer"],Perform:["pressure","transfer","baseline"]};
const blankIntake=(id:string):Intake=>({player_id:id,primary_goal:"",outcome_target:"",weekly_golf_minutes:180,weekly_vector_minutes:60,sessions_per_week:3,available_days:[],facilities:[],competition_dates:"",recovery_constraints:"",consent_confirmed:false});
const weekPlan=[
  [1,"Measure","Baseline, success definitions and representative testing","Initial measure"],[2,"Measure","Confirm priorities and set the programme route","Priority review"],
  [3,"Build","Develop the primary technical and movement capacities",null],[4,"Build","Progress the selected solution without losing strike or intent","Progress review"],[5,"Build","Consolidate the highest-value practice pattern",null],
  [6,"Stabilise","Improve repeatability across changing targets","Stability review"],[7,"Stabilise","Retain the solution as constraints increase","Mid-block review"],[8,"Stabilise","Validate the pattern without coaching aids","Retest"],
  [9,"Transfer","Move the skill into random and on-course tasks","Transfer review"],[10,"Transfer","Test strategy, consequence and competitive routines","Competition review"],
  [11,"Perform","Reduce volume and protect quality for performance","Readiness review"],[12,"Perform","Final performance test and next-cycle decision","Final retest"],
] as const;

export default function ProgrammePage(){
  const{profile}=usePlayerData();const[players,setPlayers]=useState<Player[]>([]),[selectedId,setSelectedId]=useState(""),[intake,setIntake]=useState<Intake|null>(null),[programme,setProgramme]=useState<Programme|null>(null),[weeks,setWeeks]=useState<Week[]>([]),[hasCoach,setHasCoach]=useState(false),[message,setMessage]=useState("");
  const canCoach=profile?.role==="coach"||profile?.role==="admin";const targetId=canCoach?selectedId:profile?.id||"";
  const loadPlayers=useCallback(async()=>{const sb=getSupabaseBrowserClient();if(!sb||!profile||!canCoach)return;const{data:links}=await sb.from("coach_player_links").select("player_id").eq("coach_id",profile.id);const ids=(links||[]).map(link=>link.player_id);if(!ids.length){setPlayers([]);return}const{data}=await sb.from("profiles").select("id,display_name,email").in("id",ids).order("display_name");const linked=(data||[])as Player[];setPlayers(linked);setSelectedId(current=>current&&linked.some(player=>player.id===current)?current:linked[0]?.id||"")},[profile,canCoach]);
  const loadProgramme=useCallback(async()=>{const sb=getSupabaseBrowserClient();if(!sb||!targetId)return;const[intakeResult,programmeResult,linkResult]=await Promise.all([sb.from("programme_intakes").select("*").eq("player_id",targetId).maybeSingle(),sb.from("programmes").select("*").eq("player_id",targetId).in("status",["draft","published","completed"]).order("created_at",{ascending:false}).limit(1).maybeSingle(),canCoach?Promise.resolve({data:[]}):sb.from("coach_player_links").select("coach_id").eq("player_id",targetId)]);setHasCoach(!canCoach&&!!linkResult.data?.length);setIntake((intakeResult.data as Intake|null)||blankIntake(targetId));const found=programmeResult.data as Programme|null;setProgramme(found);if(found){const{data}=await sb.from("programme_weeks").select("*").eq("programme_id",found.id).order("week_number");setWeeks((data||[])as Week[])}else setWeeks([])},[targetId,canCoach]);
  useEffect(()=>{const timer=setTimeout(()=>void loadPlayers(),0);return()=>clearTimeout(timer)},[loadPlayers]);useEffect(()=>{const timer=setTimeout(()=>void loadProgramme(),0);return()=>clearTimeout(timer)},[loadProgramme]);
  const saveIntake=async(event:React.FormEvent)=>{event.preventDefault();const sb=getSupabaseBrowserClient();if(!sb||!intake||!profile||canCoach)return;const{error}=await sb.from("programme_intakes").upsert({...intake,completed_at:new Date().toISOString(),updated_at:new Date().toISOString()});setMessage(error?error.message:"Programme questionnaire saved for your coach.")};
  const createProgramme=async()=>{
    const sb=getSupabaseBrowserClient();if(!sb||!profile||!targetId||!intake)return;
    const selfDirected=!canCoach;
    if(selfDirected&&hasCoach){setMessage("Your linked coach controls the programme. Ask them to generate or amend the plan.");return}
    const start=new Date();start.setDate(start.getDate()+((8-start.getDay())%7));
    const{data,error}=await sb.from("programmes").insert({player_id:targetId,coach_id:profile.id,planning_mode:selfDirected?"self_directed":"coach_led",primary_goal:intake.primary_goal||"Improve scoring performance",start_date:start.toISOString().slice(0,10),golf_minutes_per_week:intake.weekly_golf_minutes,vector_minutes_per_week:intake.weekly_vector_minutes}).select("*").single();
    if(error||!data){setMessage(error?.message||"Could not create programme.");return}
    const rows=weekPlan.map(([week_number,phase,focus,review_type])=>({programme_id:data.id,week_number,phase,focus,golf_minutes:intake.weekly_golf_minutes,vector_minutes:intake.weekly_vector_minutes,review_type,status:"draft"}));
    const{data:createdWeeks,error:weekError}=await sb.from("programme_weeks").insert(rows).select("id,week_number,phase,focus,golf_minutes,vector_minutes");
    if(weekError||!createdWeeks){setMessage(weekError?.message||"Could not create the programme weeks.");await loadProgramme();return}
    if(!selfDirected){setMessage("12-week programme created as a coach draft.");await loadProgramme();return}

    const[libraryResult,resultRows,detailedRows,caseRows]=await Promise.all([
      sb.from("library_items").select("id,code,title,item_type,category,stage,purpose,dosage,pass_criterion,equipment,guardrails,instruction_complete").eq("status","approved").eq("instruction_complete",true),
      sb.from("shot_band_results").select("shot_band,opportunities,successes").eq("player_id",targetId),
      sb.from("detailed_shots").select("shot_band,success,miss_length,miss_direction").eq("player_id",targetId),
      sb.from("diagnostic_cases").select("id,priority_score,evidence_summary,case_recommendations(rationale,library_items(id,code,title,item_type,category,stage,purpose,dosage,pass_criterion,equipment,guardrails,instruction_complete))").eq("player_id",targetId).eq("status","approved"),
    ]);
    const library=(libraryResult.data||[])as RecommendationLibraryItem[];
    const aggregateMap:Record<string,{shot_band:string;opportunities:number;successes:number}>={};
    for(const row of resultRows.data||[]){const aggregate=aggregateMap[row.shot_band]||={shot_band:row.shot_band,opportunities:0,successes:0};aggregate.opportunities+=row.opportunities||0;aggregate.successes+=row.successes||0}
    const missMap:Record<string,MissAggregate>={};
    for(const shot of detailedRows.data||[]){const miss=missMap[shot.shot_band]||={shot_band:shot.shot_band,failures:0,short:0,long:0,left:0,right:0};if(!shot.success){miss.failures++;if(shot.miss_length==="short")miss.short++;if(shot.miss_length==="long")miss.long++;if(shot.miss_direction==="left")miss.left++;if(shot.miss_direction==="right")miss.right++}}
    const observations=buildDiagnosticObservations(Object.values(aggregateMap),Object.values(missMap));
    const approved:ApprovedPrescription[]=[];
    for(const diagnostic of caseRows.data||[])for(const rec of diagnostic.case_recommendations||[]){const raw=rec.library_items as unknown;const item=(Array.isArray(raw)?raw[0]:raw)as RecommendationLibraryItem|null;if(item?.instruction_complete&&!approved.some(entry=>entry.item.id===item.id))approved.push({item,caseId:diagnostic.id,priorityScore:Number(diagnostic.priority_score||0),rationale:rec.rationale||diagnostic.evidence_summary})}
    const plans=new Map<number,ReturnType<typeof recommendPlanItems>>();
    for(const week of createdWeeks)plans.set(week.week_number,recommendPlanItems({library,observations,approved,phase:week.phase,facilities:intake.facilities,recoveryConstraints:intake.recovery_constraints||""}));
    const complete=createdWeeks.every(week=>{const plan=plans.get(week.week_number);return !!plan?.golf.length&&(week.vector_minutes===0||!!plan.vector.length)});
    if(!complete){setMessage("Vector created the 12-week framework, but there is not yet enough usable player data or suitable library content to populate every session. Record more evidence, then build the suggested weeks in Sessions.");await loadProgramme();return}
    const sessionCount=Math.min(7,Math.max(1,intake.sessions_per_week||3));
    const sessionRows=createdWeeks.flatMap(week=>Array.from({length:sessionCount},(_,index)=>({programme_week_id:week.id,session_number:index+1,title:`${week.phase} session ${index+1}`,objective:week.focus,status:"draft"})));
    const{data:createdSessions,error:sessionError}=await sb.from("programme_sessions").insert(sessionRows).select("id,programme_week_id,session_number");
    if(sessionError||!createdSessions){setMessage(sessionError?.message||"Could not build the recommended sessions.");await loadProgramme();return}
    const weekById=new Map(createdWeeks.map(week=>[week.id,week]));
    const blocks:Array<Record<string,unknown>>=[];
    for(const session of createdSessions){const week=weekById.get(session.programme_week_id);if(!week)continue;const plan=plans.get(week.week_number)!;const index=session.session_number-1;const golfSplit=splitMinutes(week.golf_minutes,sessionCount),vectorCount=Math.min(2,sessionCount),vectorSplit=splitMinutes(week.vector_minutes,vectorCount);const golf=recommendationForSession(plan.golf,week.week_number,index);if(golfSplit[index]>0)blocks.push({session_id:session.id,sequence:1,domain:"golf",stage:(stageByPhase[week.phase]||["skill"])[index%(stageByPhase[week.phase]||["skill"]).length],minutes:golfSplit[index],library_item_id:golf?.item.id,source_case_id:golf?.sourceCaseId||null,instructions:golf?.item.purpose,success_criterion:golf?.item.pass_criterion,recommendation_source:golf?.sourceCaseId?"coach_approved":"vector_engine",recommendation_rationale:golf?.rationale,recommendation_score:golf?.score,evidence_snapshot:golf?.evidence});if(index<vectorCount&&vectorSplit[index]>0){const vector=recommendationForSession(plan.vector,week.week_number,index);blocks.push({session_id:session.id,sequence:2,domain:"vector",stage:"vector",minutes:vectorSplit[index],library_item_id:vector?.item.id,source_case_id:vector?.sourceCaseId||null,instructions:vector?.item.purpose,success_criterion:vector?.item.pass_criterion,recommendation_source:vector?.sourceCaseId?"coach_approved":"vector_engine",recommendation_rationale:vector?.rationale,recommendation_score:vector?.score,evidence_snapshot:vector?.evidence})}}
    const{error:blockError}=await sb.from("session_blocks").insert(blocks);
    if(blockError){setMessage(blockError.message);await loadProgramme();return}
    const needsReview=(intake.recovery_constraints||"").trim().length>0||[...plans.values()].some(plan=>plan.requiresReview);
    if(!needsReview){const weekIds=createdWeeks.map(week=>week.id);const{error:publishWeekError}=await sb.from("programme_weeks").update({status:"published",updated_at:new Date().toISOString()}).in("id",weekIds);if(!publishWeekError){await sb.from("programme_sessions").update({status:"published",updated_at:new Date().toISOString()}).in("programme_week_id",weekIds);await sb.from("programmes").update({status:"published",published_at:new Date().toISOString(),recommendation_generated_at:new Date().toISOString(),recommendation_snapshot:{evidence:observations.slice(0,3).map(item=>({band:item.band,priority_score:item.priorityScore,confidence:item.confidence})),facilities:intake.facilities}}).eq("id",data.id)}else{setMessage(publishWeekError.message);await loadProgramme();return}}
    setMessage(needsReview?"Vector produced all 12 weeks as a draft. Review the suggested Vector exercises against your recovery or pain information before publishing.":"Vector produced and published your personalised 12-week practice plan. You can change any suggested drill or exercise in Sessions.");
    await loadProgramme()
  };
  const updateProgramme=async(changes:Partial<Programme>)=>{const sb=getSupabaseBrowserClient();if(!sb||!programme)return;const publish=changes.status==="published"?{published_at:new Date().toISOString()}:{};const{error}=await sb.from("programmes").update({...changes,...publish,updated_at:new Date().toISOString()}).eq("id",programme.id);if(!error&&changes.status==="published")await sb.from("programme_weeks").update({status:"published",updated_at:new Date().toISOString()}).eq("programme_id",programme.id);setMessage(error?error.message:"Programme updated.");if(!error)await loadProgramme()};
  const updateWeek=async(id:string,changes:Partial<Week>)=>{const sb=getSupabaseBrowserClient();if(!sb)return;const{error}=await sb.from("programme_weeks").update({...changes,updated_at:new Date().toISOString()}).eq("id",id);setMessage(error?error.message:"Week updated.");if(!error)await loadProgramme()};
  const totalGolf=useMemo(()=>weeks.reduce((sum,week)=>sum+week.golf_minutes,0),[weeks]),totalVector=useMemo(()=>weeks.reduce((sum,week)=>sum+week.vector_minutes,0),[weeks]);
  return <AppShell active="programme"><header className="page-heading"><div><p className="eyebrow">Data-led programme planning</p><h1>Build the next twelve weeks.</h1><p>Vector ranks the player’s evidence, proposes the most suitable drills and exercises, and keeps every recommendation editable.</p></div></header>{message&&<div className="success-banner" role="status">{message}</div>}{canCoach&&<section className="programme-player"><label>Programme player<select value={selectedId} onChange={event=>setSelectedId(event.target.value)}><option value="">Choose linked player</option>{players.map(player=><option value={player.id} key={player.id}>{player.display_name||player.email}</option>)}</select></label><span>{programme?`${programme.status} programme`:"No programme yet"}</span></section>}{targetId&&intake&&!canCoach&&<form className="intake-card" onSubmit={saveIntake}><div className="section-heading"><div><p className="eyebrow">Player questionnaire</p><h2>Make the plan fit real life.</h2></div></div><div className="intake-grid"><label>Primary goal<input value={intake.primary_goal} onChange={event=>setIntake({...intake,primary_goal:event.target.value})} required/></label><label>12-week outcome target<input value={intake.outcome_target} onChange={event=>setIntake({...intake,outcome_target:event.target.value})}/></label><label>Golf practice minutes per week<input type="number" min="0" value={intake.weekly_golf_minutes} onChange={event=>setIntake({...intake,weekly_golf_minutes:Number(event.target.value)})}/></label><label>Vector minutes per week<input type="number" min="0" value={intake.weekly_vector_minutes} onChange={event=>setIntake({...intake,weekly_vector_minutes:Number(event.target.value)})}/></label><label>Sessions per week<input type="number" min="1" max="7" value={intake.sessions_per_week} onChange={event=>setIntake({...intake,sessions_per_week:Number(event.target.value)})}/></label><label>Competition dates<textarea value={intake.competition_dates} onChange={event=>setIntake({...intake,competition_dates:event.target.value})}/></label><label className="wide">Recovery, pain or schedule constraints<textarea value={intake.recovery_constraints} onChange={event=>setIntake({...intake,recovery_constraints:event.target.value})}/></label></div><fieldset><legend>Available days</legend>{days.map(day=><label key={day}><input type="checkbox" checked={intake.available_days.includes(day)} onChange={()=>setIntake({...intake,available_days:intake.available_days.includes(day)?intake.available_days.filter(item=>item!==day):[...intake.available_days,day]})}/>{day.slice(0,3)}</label>)}</fieldset><fieldset><legend>Facilities available</legend>{facilities.map(item=><label key={item}><input type="checkbox" checked={intake.facilities.includes(item)} onChange={()=>setIntake({...intake,facilities:intake.facilities.includes(item)?intake.facilities.filter(value=>value!==item):[...intake.facilities,item]})}/>{item}</label>)}</fieldset><label className="consent"><input type="checkbox" checked={intake.consent_confirmed} onChange={event=>setIntake({...intake,consent_confirmed:event.target.checked})} required/>I confirm this information can be used to generate and adjust my programme.</label><button className="primary-action">Save questionnaire</button></form>}{canCoach&&targetId&&intake&&!programme&&<section className="programme-empty"><p className="eyebrow">Vector recommendation ready</p><h2>{intake.primary_goal||"Player questionnaire not completed"}</h2><p>{intake.weekly_golf_minutes} golf minutes + {intake.weekly_vector_minutes} Vector minutes per week · {intake.sessions_per_week} sessions</p><button className="primary-action" onClick={createProgramme} disabled={!intake.primary_goal}>Create data-led 12-week draft</button></section>}{!canCoach&&targetId&&intake&&!programme&&!hasCoach&&intake.consent_confirmed&&<section className="programme-empty"><p className="eyebrow">Self-directed Vector plan</p><h2>Let Vector produce your next 12 weeks</h2><p>The plan will use your recorded performance data, available facilities and practice time. Every drill remains changeable.</p><button className="primary-action" onClick={createProgramme} disabled={!intake.primary_goal}>Generate my 12-week plan</button></section>}{!canCoach&&hasCoach&&!programme&&<section className="programme-empty"><p className="eyebrow">Coach-led programme</p><h2>Your coach will review Vector’s suggestions</h2><p>Your data is ready for your coach to generate, amend and release the plan.</p></section>}{programme&&<><section className="programme-summary"><div><span>Status</span><strong>{programme.status}</strong></div><div><span>Golf allocation</span><strong>{totalGolf} min</strong></div><div><span>Vector allocation</span><strong>{totalVector} min</strong></div><div><span>Start</span><strong>{new Date(programme.start_date+"T12:00:00").toLocaleDateString("en-GB")}</strong></div>{canCoach&&<button className="primary-action" onClick={()=>updateProgramme({status:programme.status==="published"?"draft":"published"})}>{programme.status==="published"?"Return to draft":"Publish programme"}</button>}</section><section className="programme-timeline">{weeks.map(week=><article className={`programme-week phase-${week.phase.toLowerCase()}`} key={week.id}><header><span>Week {String(week.week_number).padStart(2,"0")}</span><b>{week.phase}</b>{week.review_type&&<small>{week.review_type}</small>}</header><div><h2>{week.focus}</h2>{canCoach?<><label>Weekly focus<input value={week.focus} onChange={event=>setWeeks(current=>current.map(item=>item.id===week.id?{...item,focus:event.target.value}:item))} onBlur={event=>updateWeek(week.id,{focus:event.target.value})}/></label><div className="minute-fields"><label>Golf minutes<input type="number" min="0" value={week.golf_minutes} onChange={event=>setWeeks(current=>current.map(item=>item.id===week.id?{...item,golf_minutes:Number(event.target.value)}:item))} onBlur={event=>updateWeek(week.id,{golf_minutes:Number(event.target.value)})}/></label><label>Vector minutes<input type="number" min="0" value={week.vector_minutes} onChange={event=>setWeeks(current=>current.map(item=>item.id===week.id?{...item,vector_minutes:Number(event.target.value)}:item))} onBlur={event=>updateWeek(week.id,{vector_minutes:Number(event.target.value)})}/></label></div><label>Coach note<textarea defaultValue={week.coach_notes||""} onBlur={event=>updateWeek(week.id,{coach_notes:event.target.value})}/></label></>:<><p>{week.golf_minutes} golf minutes · {week.vector_minutes} Vector minutes</p>{week.coach_notes&&<blockquote>{week.coach_notes}</blockquote>}</>}</div></article>)}</section></>}{!targetId&&<div className="empty-state compact"><h2>Select a linked player</h2><p>Choose a player to build or edit their 12-week programme.</p></div>}</AppShell>;
}
