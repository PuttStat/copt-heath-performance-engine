import { AppShell } from "../ui/app-shell";
export default function Performance() { return <AppShell active="performance"><header className="page-heading"><div><p className="eyebrow">Performance engine</p><h1>Priorities, weighted by play.</h1><p>A weak result only becomes urgent when the shot occurs often enough to affect scoring.</p></div></header><section className="analysis-table"><div className="analysis-row header"><span>Shot band</span><span>Frequency</span><span>Success</span><span>Priority</span></div>{[["125–150 yd","18%","48%","High"],["Putting 6–15 ft","23%","64%","Medium"],["Tee shot","14%","71%","Maintain"],["Bunker","2%","0%","Low evidence"]].map((row) => <div className="analysis-row" key={row[0]}>{row.map((cell, i) => <span key={cell} className={i === 3 ? `status ${cell.toLowerCase().replace(" ", "-")}` : ""}>{cell}</span>)}</div>)}</section></AppShell>; }
