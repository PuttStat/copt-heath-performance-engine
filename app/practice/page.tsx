import { AppShell } from "../ui/app-shell";
export default function Practice() { return <AppShell active="practice"><header className="page-heading"><div><p className="eyebrow">Week 1</p><h1>Practice with a reason.</h1><p>Each block connects a scoring priority, diagnostic clue and Vector movement objective.</p></div></header><div className="session-list">{[["01","Calibrate carry","Build a trustworthy 125 / 140 / 150-yard baseline","8 min"],["02","Random distance ladder","Change target every ball; score the outcome against your own expectation","12 min"],["03","Lead-side pressure","Make the movement easier before adding speed","8 min"]].map((item) => <article className="session-card" key={item[0]}><span>{item[0]}</span><div><p className="eyebrow">Practice block</p><h2>{item[1]}</h2><p>{item[2]}</p></div><strong>{item[3]}</strong></article>)}</div></AppShell>; }
